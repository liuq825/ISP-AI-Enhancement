# CPU Teacher 烟雾训练：只验证 Teacher checkpoint 可由版本化命令生成。
seed: 20260728
device: cpu
model_config: configs/model_smoke.yaml
context_config: configs/context.yaml
manifest: data/local_phase2_smoke/manifest.jsonl
output_dir: runs/local_teacher_smoke
data_policy:
  purpose: smoke
  catalog: resources/datasets.yaml
epochs: 1
batch_size: 1
num_workers: 0
crop_size: 64
learning_rate: 0.0002
weight_decay: 0.0
amp: false
log_every: 20
save_every_epochs: 1
loss:
  charbonnier: 1.0
  gradient: 0.1
  color: 0.05
