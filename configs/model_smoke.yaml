# 极小 NAFNet，仅用于 CPU 冒烟测试，不能作为质量或性能结论。
model:
  name: nafnet_raw
  input_channels: 16
  output_channels: 4
  width: 8
  encoder_blocks: [1, 1, 1, 1]
  middle_blocks: 1
  decoder_blocks: [1, 1, 1, 1]
  expansion_spec: baseline
