# 未剪枝 Student P0：enc3 增加到 6 个 block，提升中尺度纹理与噪声建模容量。
# 块数仍由 YAML 配置驱动，并非写死在训练/导出逻辑中。
model:
  name: nafnet_raw
  input_channels: 16
  output_channels: 4
  width: 32
  encoder_blocks: [2, 2, 6, 8]
  middle_blocks: 4
  decoder_blocks: [2, 2, 2, 2]
  expansion_spec: baseline
