# 宽教师模型，仅在训练期提供输出与特征监督，不进入麒麟 9000 部署包。
model:
  name: nafnet_raw
  input_channels: 16
  output_channels: 4
  width: 64
  encoder_blocks: [2, 2, 4, 8]
  middle_blocks: 4
  decoder_blocks: [2, 2, 2, 2]
  expansion_spec: baseline
