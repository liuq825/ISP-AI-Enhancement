# 剪枝后 QAT 烟雾配置：使用恢复微调后的权重，而不是刚剪枝的权重。
seed: 20260728
device: cpu
model_config: configs/model_smoke_structaware15.yaml
initial_checkpoint: runs/local_structaware15_finetune_smoke/best.pt
qat_config: configs/qat.yaml
context_config: configs/context.yaml
manifest: data/local_phase2_smoke/manifest.jsonl
output_dir: runs/local_structaware15_qat_smoke
data_policy:
  purpose: smoke
  catalog: resources/datasets.yaml
epochs: 1
batch_size: 1
num_workers: 0
crop_size: 64
learning_rate: 0.00002
weight_decay: 0.0
amp: false
log_every: 20
save_every_epochs: 1
loss:
  charbonnier: 1.0
  gradient: 0.1
  color: 0.05
